spring:
  profiles:
    active:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:~/deploy
    driver-class-name: org.h2.Driver
    username: deploy
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true